import { IconButton } from "react-native-paper"

export default function AddButton() {
    
        return (
            <IconButton containerColor="#e1e1e1" icon="plus-thick" mode="contained" compact="true">
                
            </IconButton>
        )
}