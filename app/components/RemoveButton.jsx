import { IconButton } from "react-native-paper"

export default function RemoveButton() {
    
        return (
            <IconButton containerColor="#e1e1e1" icon="delete" mode="contained" compact="true">
                
            </IconButton>
        )
}